import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  // Estilos para MyCamera
  
  // Estilos para AddPost
  
  // Estilos para Home
  textHome: {
    fontFamily: 'Arial, sans-serif',
    color: '#2c3e50',
    fontWeight: 'bold',
    fontSize: 35,
    textAlign: 'center',
    backgroundColor: '#ecf0f1',
    marginBottom: 20,
    marginTop: 20,
  },

  // Estilos para MiPerfil
  
  // Estilos compartidos
  text: {
    fontFamily: 'Arial, sans-serif',
    color: '#2c3e50',
    fontSize: 20,
    flexDirection: 'column',
  },
});

export default styles;





